# Build artifacts
*.exe
throttle-router
router

# Composed schema (generated)
config.json

# Env / secrets
.env
*.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# ...except this one, which is committed so the use case runs out of the box.
# Regenerate with: wgc router compose -i graph.yaml -o config.json
!01-parallel-requests-and-response-stitching/01-n-plus-1-hydration/router-config/config.json
!01-parallel-requests-and-response-stitching/02-scatter-gather-composition/router-config/config.json
